#include "main.h"

/** _isupper - check the code for uppercase.
 *@x: The number to be checked
 * Return: Always 0.
 */
int _isupper(int x)
{
	if (x >= 65 && x < 90)
	{
	return (1);
	}
	return (0);
}

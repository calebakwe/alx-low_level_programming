#include <stdio.h>
#include <stdlib.h>

/**
 * main- return number
 *
 * Return : Always 0 (Success)
 */

int main(void)

{
      	 char  i = '0';

        for (i = 0; i <= 9; i++)
        {
                printf("%d", i);
        }
        return (0);
}
